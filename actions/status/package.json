{
  "name": "@vercel/repository-dispatch-status",
  "version": "1.0.0",
  "description": "GitHub action that sets the status of the workflow on the sha received from a repository_dispatch event",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "prepare": "pnpm run build"
  },
  "author": "Vercel",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.3.0"
  }
}
