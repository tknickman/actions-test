{
  "name": "set-status-and-run-command",
  "version": "1.0.0",
  "description": "GitHub Action to set status on HEAD, run a command, and update the status to passed or failed.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "pnpm run build"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
